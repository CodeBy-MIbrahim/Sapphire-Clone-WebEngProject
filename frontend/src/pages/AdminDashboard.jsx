import React from 'react';

const AdminDashboard = () => {
    return (
        <div className="container mt-5">
            <h1>Admin Dashboard</h1>
            <p>This is where the CRUD magic happens.</p>
        </div>
    );
};

export default AdminDashboard;